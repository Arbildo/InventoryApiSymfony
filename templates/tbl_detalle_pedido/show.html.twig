{% extends 'base.html.twig' %}

{% block title %}TblDetallePedido{% endblock %}

{% block body %}
    <h1>TblDetallePedido</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDetallePedido</th>
                <td>{{ tbl_detalle_pedido.idDetallePedido }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ tbl_detalle_pedido.cantidad }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ tbl_detalle_pedido.precio }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_detalle_pedido.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_detalle_pedido_index') }}">back to list</a>

    <a href="{{ path('tbl_detalle_pedido_edit', {'idDetallePedido': tbl_detalle_pedido.idDetallePedido}) }}">edit</a>

    {{ include('tbl_detalle_pedido/_delete_form.html.twig') }}
{% endblock %}
