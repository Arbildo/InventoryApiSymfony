<form method="post" action="{{ path('tbl_unidad_medida_delete', {'idUnidad': tbl_unidad_medida.idUnidad}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_unidad_medida.idUnidad) }}">
    <button class="btn">Delete</button>
</form>
