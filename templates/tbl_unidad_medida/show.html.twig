{% extends 'base.html.twig' %}

{% block title %}TblUnidadMedida{% endblock %}

{% block body %}
    <h1>TblUnidadMedida</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdUnidad</th>
                <td>{{ tbl_unidad_medida.idUnidad }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tbl_unidad_medida.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_unidad_medida.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_unidad_medida_index') }}">back to list</a>

    <a href="{{ path('tbl_unidad_medida_edit', {'idUnidad': tbl_unidad_medida.idUnidad}) }}">edit</a>

    {{ include('tbl_unidad_medida/_delete_form.html.twig') }}
{% endblock %}
