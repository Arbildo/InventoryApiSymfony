<form method="post" action="{{ path('tbl_perdida_delete', {'idPerdida': tbl_perdida.idPerdida}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_perdida.idPerdida) }}">
    <button class="btn">Delete</button>
</form>
