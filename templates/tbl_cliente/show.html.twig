{% extends 'base.html.twig' %}

{% block title %}TblCliente{% endblock %}

{% block body %}
    <h1>TblCliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCliente</th>
                <td>{{ tbl_cliente.idCliente }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ tbl_cliente.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tbl_cliente.nombre }}</td>
            </tr>
            <tr>
                <th>NumeroDoc</th>
                <td>{{ tbl_cliente.numeroDoc }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ tbl_cliente.direccion }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ tbl_cliente.correo }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ tbl_cliente.telefono }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_cliente.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_cliente_index') }}">back to list</a>

    <a href="{{ path('tbl_cliente_edit', {'idCliente': tbl_cliente.idCliente}) }}">edit</a>

    {{ include('tbl_cliente/_delete_form.html.twig') }}
{% endblock %}
