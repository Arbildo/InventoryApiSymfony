<form method="post" action="{{ path('tbl_atendido_delete', {'idAtendido': tbl_atendido.idAtendido}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_atendido.idAtendido) }}">
    <button class="btn">Delete</button>
</form>
