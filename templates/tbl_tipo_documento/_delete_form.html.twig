<form method="post" action="{{ path('tbl_tipo_documento_delete', {'idTipo': tbl_tipo_documento.idTipo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_tipo_documento.idTipo) }}">
    <button class="btn">Delete</button>
</form>
