{% extends 'base.html.twig' %}

{% block title %}TblTipoDocumento{% endblock %}

{% block body %}
    <h1>TblTipoDocumento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTipo</th>
                <td>{{ tbl_tipo_documento.idTipo }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ tbl_tipo_documento.tipo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_tipo_documento.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_tipo_documento_index') }}">back to list</a>

    <a href="{{ path('tbl_tipo_documento_edit', {'idTipo': tbl_tipo_documento.idTipo}) }}">edit</a>

    {{ include('tbl_tipo_documento/_delete_form.html.twig') }}
{% endblock %}
