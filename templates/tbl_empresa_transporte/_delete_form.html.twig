<form method="post" action="{{ path('tbl_empresa_transporte_delete', {'idEmpresa': tbl_empresa_transporte.idEmpresa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_empresa_transporte.idEmpresa) }}">
    <button class="btn">Delete</button>
</form>
