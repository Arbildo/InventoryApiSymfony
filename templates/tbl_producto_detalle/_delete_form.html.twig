<form method="post" action="{{ path('tbl_producto_detalle_delete', {'idProductoDetalle': tbl_producto_detalle.idProductoDetalle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_producto_detalle.idProductoDetalle) }}">
    <button class="btn">Delete</button>
</form>
