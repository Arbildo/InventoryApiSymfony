{% extends 'base.html.twig' %}

{% block title %}TblDetalleOrdenCompra{% endblock %}

{% block body %}
    <h1>TblDetalleOrdenCompra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDetalle</th>
                <td>{{ tbl_detalle_orden_compra.idDetalle }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ tbl_detalle_orden_compra.precio }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ tbl_detalle_orden_compra.cantidad }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_detalle_orden_compra.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_detalle_orden_compra_index') }}">back to list</a>

    <a href="{{ path('tbl_detalle_orden_compra_edit', {'idDetalle': tbl_detalle_orden_compra.idDetalle}) }}">edit</a>

    {{ include('tbl_detalle_orden_compra/_delete_form.html.twig') }}
{% endblock %}
