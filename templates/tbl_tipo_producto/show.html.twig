{% extends 'base.html.twig' %}

{% block title %}TblTipoProducto{% endblock %}

{% block body %}
    <h1>TblTipoProducto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTipo</th>
                <td>{{ tbl_tipo_producto.idTipo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tbl_tipo_producto.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_tipo_producto.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_tipo_producto_index') }}">back to list</a>

    <a href="{{ path('tbl_tipo_producto_edit', {'idTipo': tbl_tipo_producto.idTipo}) }}">edit</a>

    {{ include('tbl_tipo_producto/_delete_form.html.twig') }}
{% endblock %}
