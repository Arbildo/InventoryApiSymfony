{% extends 'base.html.twig' %}

{% block title %}TblTalla{% endblock %}

{% block body %}
    <h1>TblTalla</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTalla</th>
                <td>{{ tbl_talla.idTalla }}</td>
            </tr>
            <tr>
                <th>NombreTalla</th>
                <td>{{ tbl_talla.nombreTalla }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tbl_talla.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tbl_talla_index') }}">back to list</a>

    <a href="{{ path('tbl_talla_edit', {'idTalla': tbl_talla.idTalla}) }}">edit</a>

    {{ include('tbl_talla/_delete_form.html.twig') }}
{% endblock %}
