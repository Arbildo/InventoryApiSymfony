<form method="post" action="{{ path('tbl_detalle_nota_credito_delete', {'idDetalleNota': tbl_detalle_nota_credito.idDetalleNota}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_detalle_nota_credito.idDetalleNota) }}">
    <button class="btn">Delete</button>
</form>
