<form method="post" action="{{ path('tbl_guia_salida_delete', {'idGuiaSalida': tbl_guia_salida.idGuiaSalida}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_guia_salida.idGuiaSalida) }}">
    <button class="btn">Delete</button>
</form>
