<form method="post" action="{{ path('tbl_usuario_delete', {'idUsuario': tbl_usuario.idUsuario}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_usuario.idUsuario) }}">
    <button class="btn">Delete</button>
</form>
