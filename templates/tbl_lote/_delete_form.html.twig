<form method="post" action="{{ path('tbl_lote_delete', {'idLote': tbl_lote.idLote}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_lote.idLote) }}">
    <button class="btn">Delete</button>
</form>
